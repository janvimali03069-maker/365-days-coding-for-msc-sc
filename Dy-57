#include<stdio.h>
  int main()
{
int N,i,count=0,sum=0;
  printf("Enter N:");
  scanf("%d",&N);
  for(i=1; i<N; i++)
  {
    if
      ((i%3==0)^(i%5==0))
    {
      printf("%d",i);
      count++;
      sum+=i;
    }
  }
  printf("\nCount=%d",count);
  printf("\nSum=%d",sum);
  return 0;
}
