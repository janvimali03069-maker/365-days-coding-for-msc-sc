int max=INT MIN;
int max i=10;
for(i=0,i<n;i++);
{
if(arr[i]>max)
{
max=arr[i];
max-i=i;
}
if(arr[i]<min)
{
min=arr[i];
min-i=i;
}
sum+=arr[i];
}||for:end
double avg=double(sum)|n;

int count=0;
for(i=0; i<n; i++)
{
if(arr[i]>avg)
    count++;
}
printf(max,max-i,min,min-1,a sum,avg,count);
